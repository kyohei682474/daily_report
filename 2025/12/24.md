# 2025/12/24の日報
## 取り組んだ課題一覧
* タイピング練習
## わかったこと 
* keyofはオブジェクトのキーをユニオン型で受け取る演算子
  * コンパイル時にキーをユニオン型で返す
  * type ofを組み合わせることで値を型に変換してからkeyofで型を受け取ることができる。また型だけなくオブジェクトも返す。
  * ポイントはkey ofは型の世界の話をしていて、type ofは値の世界の話をしている。値を見て型を作成する　
  * value ofは値だけを取り出す。
* T[key of T]とすることでTの型のそのキーに対応する値を全て取り出す
 * Tは何かを入れるための箱でジェネリクスと呼ばれている
 * ジェネリクスをkey ofすることでオブジェクトのキーをユニオン型で受け取ることができる
* javascriptは値の世界、Typescriptは方の世界
* Typescriptはコンパイルの時だけ存在する設計図
* 演算子は何かを受け取って何かを返すものである。
  * typeofは演算子だから値を受け取って型を返すという演算子
* conditional Typesは条件付き型のことで、type Result = T extends U ? X : Y;
* typeは型チェックに使用される
* inferは型を取り出して配列の中身に対して名前をつける
* 
  
  
      
##  合計時間 
Today: 2.0h<br>
Total: 1875.55
