# 2025/12/18の日報
## 取り組んだ課題一覧
* タイピング練習
## わかったこと 
* SWRは古いデータを返してその裏で新しいデータを取得して最終的に新しいデータで更新を行う
  * キャッシュ、エラーハンドリング、再検証など自動で行われる
  * SWRがないと違う名前の同じ内容の関数(自動キャッシュと再検証)を２つ用意しなくてはいけなくなってしまう
  * キーとキャッシュの共有
  * フォーカス時の再検証
  * ポーリング(定期的な再検証)
  * 条件付きフェッチ(例えばuserIdがない場合はfetchしない）
    * const { data } = useSWR(userId ? `/api/users/${userId}/goals` : null,fetcher);
    * fetchはURLからデータを取得してくるということ。
* @ = src
* git reflogは自分が何をしたかを把握するものでローカル専用で使用する
* 
## 次やること
* 
##  合計時間 
Today: 2.0h<br>
Total: 1875.55
