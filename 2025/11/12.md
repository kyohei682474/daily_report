# 2025/11/12の日報
## 取り組んだ課題一覧
* タイピング練習
## わかったこと 
* sortメソッドは配列.sort((a, b) => {戻り値で並び順が決まる、負の数 → aをbの前に、0     → 順番そのまま、正の数 → bをaの前に});
  * a-bなら昇順
  * b-aなら降順
* sort内部がやっていること
  * 配列[0] と 配列[1] を比較 → a = 配列[0], b = 配列[1]
  * 配列[1] と 配列[2] を比較 → a = 配列[1], b = 配列[2]
  * 配列[0] と 配列[2] を比較 → a = 配列[0], b = 配列[2]
* getTime()で日付を数値に変更する
  * 日付では数値の比較ができないから
*　      
## 次やること
* 
##  合計時間 
Today: 1.5h<br>
Total: 1868.55
